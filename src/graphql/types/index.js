import { User, UserMutation, UserQuery } from "./User";
import { Wish, WishMutation, WishQuery } from "./Wish";

export default {
	User,
	// UserMutation,
	UserQuery,
	Wish,
	// WishMutation,
	WishQuery,
};
